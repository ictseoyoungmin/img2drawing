# Observed mechanical example

This deterministic synthetic reference demonstrates create → observe → author → inspect →
correct → finish → PNG/replay → resume through `DrawingSession`.

```bash
python examples/observed/run.py --output /tmp/img2drawing-observed
```

It verifies integration mechanics only. It is not an answer image, subject benchmark, or
claim of visual quality.
