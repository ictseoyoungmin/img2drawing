# Legacy R23 stage references

이 디렉터리는 `references/legacy-r23.md`가 가리키는 R23 compatibility 자료다.
새 작업의 reading route에 포함하지 않는다.

`p1-*`부터 `p6-*`, `stage-contracts.md`, 동봉 이미지는 기존 `DrawingRun`의
stage-oriented representation vocabulary와 regression evidence를 보존한다. 이
파일들의 Pn, stage, advance, reopen 의미는 vNext API·mode·finish lifecycle로
재사용하지 않는다.
