# B05 reopen — Canonical Pn guidance de-anchoring

Formal state: **CLOSED**
First activation/reclosure: 2026-08-31
Second activation/reclosure: 2026-08-31; sole production WIP while active
Existing closure: `../capsules/B05.md`

## Reopen record

### Reason and trigger

B05 construction implementation and visual dogfood are closed, but the universal
drawing plan requires a normal reading route that does not teach P1–P6 orchestration.
The current `skills/img2drawing/SKILL.md` still contains substantial stage review,
stage handoff, advance/reopen, and Pn detail, and it routes legacy references from the
same top-level document. This attention leak would pull tonal/free-draw work back into
the croquis stage model.

### Affected contract surface

- `skills/img2drawing/SKILL.md` normal reading route and mission wording
- `skills/img2drawing/references/INDEX.md`
- stage-coupled playbooks, review guidance, examples, and package description
- drawing knowledge currently trapped in `references/stages/p1-*.md` through `p6-*.md`
- explicit legacy R23 documentation entry point

Runtime stage/persistence removal is not part of this reopen.

## Second reopen record — physical attention boundary

### Reason and trigger

The first reclosure treated the `<details>` boundary inside `SKILL.md` as if it were a
worker reading boundary. A fresh LLM receives the file contents regardless of browser
collapse state, so the embedded R23 body could still bias new work toward P1–P6,
`advance`, or `reopen_stage` thinking.

### Affected contract surface

- the physical contents of `skills/img2drawing/SKILL.md`;
- the legacy section of `skills/img2drawing/references/INDEX.md`;
- direct evidence for a fresh worker's file reads and selected vNext APIs.

### Migration risk and boundary

Remove only the duplicate R23 prose from `SKILL.md` and the descendant inventory from
`INDEX.md`. Preserve the existing `references/legacy-r23.md` gateway and all underlying
R23 source/reference files. Do not change runtime stages, checkpoints, construction
geometry, or the accepted visual evidence.

### Migration risk

- useful gesture, mass, balance, overlap, feet, prop, identity, and accent guidance may
  be lost while removing lifecycle language;
- existing R23 continuation users may lose their compatibility instructions;
- moving paths can leave broken links or silently duplicate canonical guidance;
- broad cleanup can accidentally expand into B12/B18 runtime retirement.

### Activation boundary

This reopen changes only canonical attention/documentation and the legacy example
entrypoint. Accepted B05 construction geometry, `DrawingSession`, `InspectionSheet`,
history/checkpoint semantics, and committed B05 visual evidence remain frozen.

## Goal

A new drawing worker reads one short stage-free route, learns an ordered but non-gated
drawing grammar, and reaches the existing B04/B05 API without encountering Pn. A legacy
R23 continuation remains possible only through one clearly labeled compatibility link.

## Deliverables

1. Rewrite the canonical `SKILL.md` around observe → construct/draw → inspect → correct
   → finish; remove the current top-level “stage handoff/review” instruction blocks.
2. Create a stage-free reference taxonomy for construction, observation, resolution,
   finish, mode, and residual review guidance.
3. Extract reusable knowledge from P1–P6 before moving or labeling the old documents as
   legacy; do not copy stage ownership/advance/manifest semantics.
4. Make `references/INDEX.md`, the example, package description, and canonical links
   point to vNext-native guidance.
5. Preserve one explicit `legacy/r23` (or equivalently isolated) entry with migration
   notes for existing `DrawingRun` continuations.
6. Add a deterministic link/route audit and record a subject-only fresh-worker reading
   route check.
7. Reclose B05 with an updated capsule; preserve the existing construction evidence.

## Knowledge migration map

| Legacy source | Keep as stage-free knowledge | Target concern |
|---|---|---|
| P1 | gesture, cranium/hair, limb curvature, feet direction, occlusion | construction/observation |
| P2 | segment direction, foreshortening, torso/pelvis orientation, relations | construction |
| P3 | occupied volume, taper, rotation, negative space | construction/resolution |
| P4 | hair/face opening, garment anchors, footwear, prop contact | resolution/finish |
| P5 | selective contour, redundant-mark retirement, ownership handoff | resolution/review |
| P6 | relational face features, grouped hair, selective accent, calibration | finish |

Mode-specific advice such as “feet belong early” goes to a croquis/figure guide rather
than a universal contract.

## Definition of Closed

- [x] `STATUS.md` recorded B05 as the sole `REOPENED` slice during execution.
- [x] New-task canonical reading contains no P1–P6 progression, stage start/advance,
      stage closure, reopen, stage manifest, or downstream invalidation instruction.
- [x] Search proves every remaining Pn instruction is inside the explicit legacy area,
      compatibility source/tests, historical evidence, or a migration explanation.
- [x] Useful Pn drawing knowledge has a stage-free home and no competing canonical copy.
- [x] `SKILL.md` points new croquis/figure work to `DrawingSession` and B05 construction.
- [x] A tonal/free-draw request cannot be routed into croquis stage contracts.
- [x] Existing legacy R23 continuation instructions remain reachable from one explicit
      compatibility entry and are clearly excluded from new work.
- [x] All Markdown links and package/docs examples resolve.
- [x] vNext import isolation and focused B04/B05 tests pass; full suite remains green.
- [x] A fresh worker given only skill + subject + new drawing request reports a Pn-free
      reading trace and can identify the stage-free action loop.
- [x] Direct review confirms the instructions are shorter, unambiguous, and do not bury
      the vNext route behind legacy details.
- [x] Updated B05 capsule records outputs, limits, evidence, and reopen conditions.

## Second reopen gate checklist

- [x] `SKILL.md` contains no embedded R23 body; it has one short legacy pointer only.
- [x] `INDEX.md` names only `legacy-r23.md` in its legacy section.
- [x] A committed fresh-worker route artifact lists files read and explicitly not read.
- [x] The route audit checks the entire `SKILL.md`, not a pre-`<details>` slice.
- [x] The capsule and `STATUS.md` record this second reopen and its direct evidence.

## Non-goals

- B06 residual ranking/correction memory
- `DrawingIntent`, mode/style schemas, tonal/free-draw implementation
- R23 runtime/checkpoint removal or public compatibility break
- modification of accepted B05 construction geometry without new visual evidence
- new automated artistic PASS logic

## Handoff to B06

Hand off only a readable whole-figure construction surface, one authoritative history,
one inspection route, and Pn-free guidance. Do not hand off phase completion state.

## Reclosure record

All second-reopen gates passed on 2026-08-31. The embedded `<details>` body was physically
removed from `SKILL.md`; its only R23 content is a short explicit pointer to
`references/legacy-r23.md`. The `INDEX.md` legacy section now names only that gateway.

Evidence:

- whole-file route audit: `CANONICAL_READING_ROUTE_WHOLE_FILE_PASS`;
- single legacy gateway audit: `INDEX_SINGLE_LEGACY_GATEWAY_PASS`;
- deterministic Markdown link audit: `MARKDOWN_LINK_AUDIT_PASS`;
- package/docs path verifier: `REPOSITORY_PATH_SANITIZATION_PASS`,
  `REPOSITORY_JSON_PARSE_PASS`, `REPOSITORY_PYTHON_COMPILE_PASS`;
- fresh-worker reading artifact: [`canonical-route-fresh-worker.md`](../../../evidence/vnext/b05/canonical-route-fresh-worker.md);
- example smoke: PASS, stage-free trace and inspection sheet written under
  `/tmp/img2drawing-b05-example` using the bundled subject only;
- import isolation and compile check: `VNEXT_IMPORT_ISOLATION_PASS`;
- focused tests: `21 passed in 3.57s`;
- release identity compatibility: `R23_RELEASE_VALIDATION_PASS 0.5.2.dev23`;
- artifact identity/hash gate: `S15_VERIFICATION_PASS`;
- dedicated validator regression: `1 passed`;
- full suite: `91 passed, 5 warnings`.

The updated authoritative capsule is [`../capsules/B05.md`](../capsules/B05.md). B05 is
now closed; B06 is the next skeleton and requires explicit activation.
