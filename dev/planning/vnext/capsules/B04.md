# B04 Context Capsule — Stage-agnostic DrawingSession

Status: **CLOSED**
Closed: 2026-08-30 after provenance reopen R1–R4
Baseline: `25ec4544e86fe37fc28d64575df145a1b711d63a` (frozen R23 reference)
Archived card: `dev/planning/vnext/archive/B04.md`

## Responsibility completed

B04 delivered the minimal executable vNext session boundary while preserving the
shared R23 drawing capabilities. It did not implement B05 construction grammar,
B06 correction policy, or any later slice.

## Public surface

Use `img2drawing.vnext.DrawingSession` (also available as the lazy root alias
`img2drawing.DrawingSession`):

```text
create → observe → draw/draw_many → inspect → explicit edits → repeat → finish
```

The facade provides `create`, `resume`, `observe`, `draw`, `draw_many`,
`replace_stroke`, `replace_segment`, `soft_lift`, `delete_stroke`, `inspect`,
`checkpoint`, and `finish`. No operation accepts or branches on a workflow stage.

## Authoritative inputs and ownership

- `AgentDrawingSession` and its `CanvasHistory` are the single authoritative
  action/state substrate.
- `StrokeIR` is a read-only current-state projection.
- The facade owns session metadata, subject identity, observation snapshots,
  inspection references, finish metadata, and checkpoint persistence.
- The B02+B03 `InspectionSheet` remains the sole inspection implementation.
- The shared `pillow_pencil_contact.render` remains the sole material renderer.

The internal compatibility stage `__vnext_compat__` is opaque provenance only.
Public snapshots clear the legacy stroke stage before hashing/rendering, so the
vNext state and renderer seed are independent of legacy stage labels. Authored
`tool_state` and nested metadata are not stripped.

## Frozen invariants

1. Stage-free API: no public stage argument, stage registry, stage manifest, stage
   transition, reopen bookkeeping, or visual verdict.
2. Single history authority: mutations append through shared `DrawingAction` and
   `CanvasHistory`; public state is a defensive projection.
3. Atomic mutation boundary: each accepted observe/edit/finish operation writes a
   checkpoint while holding the session lock. A failed checkpoint restores the
   prior in-memory state; `draw_many` commits as one batch.
4. Atomic inspection binding: `inspect()` locks one snapshot, renders it to a
   temporary `raw_drawing.png`, and supplies that same snapshot as `drawing_ir` to
   `InspectionSheet.create`. The completed sheet is promoted to
   `inspections/<six-digit-id>/`; callers cannot pair arbitrary raster/hash inputs.
5. Append-only evidence: every inspection has a unique ID and immutable manifest
   directory. Earlier manifests and all their rasters remain byte-stable across
   later inspections.
6. Failed inspection commit: if checkpoint persistence fails after rendering, the
   new artifact directory and history entry are rolled back while prior inspection
   directories remain intact.
7. Subject provenance: checkpoint and inspection reject subject bytes changed after
   session creation/resume.
8. Observation provenance: IDs are unique; explicit drawing references must resolve
   to a known observation; draw-before-observe uses `vnext-unobserved`.
9. Portable persistence: checkpoint JSON schema v2 records subject basename, SHA-256,
   action history, current digest, renderer identity/version/seed domain, inspection
   history, and relative artifact references. Custom checkpoint metadata names the
   actual file. Absolute machine paths and R23 review/stage state are excluded.
10. Legacy compatibility isolation: importing `img2drawing.vnext` does not load
    `img2drawing.run` or `img2drawing.stages`; the root retains lazy legacy exports.

## Reopen resolution

- R1 immutable six-digit inspection IDs.
- R2 append-only `inspections/<id>/` artifact directories.
- R3 temporary-directory promotion and failed-checkpoint artifact cleanup.
- R4 repeated-inspection and resume verification of prior manifests/raw SHA values.
- Duplicate/unknown observation IDs are rejected; custom checkpoint filename and
  artifact-root metadata are accurate and tested.

## Evidence

- Focused B04 tests: `12 passed`.
- Full repository suite: `81 passed, 5 warnings`.
- Baseline and repository path/JSON/Python gates pass.
- Tests cover 10 stage-free strokes, all required edit operations, batch action
  identity, checkpoint rollback, repeated immutable inspections, artifact rollback,
  subject mutation rejection, observation integrity, custom checkpoint metadata,
  and resume continuity.

## Known limitations

- This slice validates state/orchestration and evidence integrity, not drawing
  likeness or visual completion.
- `finish()` stores metadata and preserves the checkpoint boundary; finish-quality
  semantics remain for a later slice.
- R23 replay remains legacy/reference-only. Its artifacts are not vNext PASS
  evidence.

## B05 handoff

B05 may add agent-authored observation/construction grammar on top of this facade.
It must keep the one-history authority, avoid stage branches, pass explicit
stage-free drawing actions, and call the existing `inspect()` path rather than
creating another renderer or inspection implementation. B05 is not activated by
this closure.

## Reopen conditions

Reopen B04 if a later change introduces stage semantics into the public facade,
breaks checkpoint atomicity or subject binding, aliases or overwrites prior
inspection evidence, loses inspection/correction continuity on resume, accepts
ambiguous observation provenance, or permits a second co-equal runtime/inspection
path.

## Authoritative source locations

- Implementation: `skills/img2drawing/src/img2drawing/vnext/session.py`
- Root import boundary: `skills/img2drawing/src/img2drawing/__init__.py`
- Tests: `dev/tests/test_vnext_session.py`
- Closed card: `dev/planning/vnext/archive/B04.md`
- B04 control plane: `dev/planning/vnext/STATUS.md`
