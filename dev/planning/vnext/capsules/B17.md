# B17 Context Capsule — package, public API, and release-candidate truth

Status: **CLOSED**
Closed: **2026-09-02**
Reopened/recosed: **B17-R1, 2026-09-02** after remote CI exposed ambient build/install assumptions
Depends: B16
Next: B18 dogfood-ready system freeze

## Release-candidate identity

The current package is `0.6.0rc1`, release slice
`B17_package_public_api_release_candidate`, with public contract
`DrawingSession/0.6.0-vnext`. `_version.py` remains build metadata authority. R23 keeps
its frozen `DrawingRun/0.5.2-r23` identity only at `img2drawing.legacy.r23`; the historical
validator now checks that preserved manifest under the current vNext package instead of
requiring the current package to impersonate R23.

## Lean artifact boundary

The wheel contains Python runtime modules, required JSON data, and LICENSE/NOTICE. The
sdist additionally includes `SKILL.md`, canonical stage-free references, release/support/
migration guidance, and two selected mechanical examples. It excludes dev/dogfood/raw
evidence/temp data, playbooks, stage reference docs, caches, the detailed subject fixture,
and answer targets. The unused 1.3 MB `p1_target.png` and duplicate stage-era
`review/reference-authority.md` were removed from source.

`SUPPORT.md` maps the canonical root API and explicit legacy boundary. `MIGRATION.md`
preserves B12's v1-v3 checkpoint support and one-way migration semantics. Normal README,
package description, examples, and CI lead to `DrawingSession`, not Pn.

## Canonical integration examples

`examples/mechanical_workflows.py` owns shared deterministic mechanics. The observed
example creates its own synthetic reference; the subjectless example uses imaginative
authority and drawing-only inspection without a fake subject, overlay, or registration.
Both perform create, observe, draw, inspect, record a material residual, replace the
responsible stroke, re-inspect, resolve, finish, render PNG, export action replay/GIF,
resume, and verify current completion provenance. They are explicitly not quality claims.

## B17-R1 remote-environment hardening

The first B17/B18 push failed on GitHub Actions because the package verifier used
`python -m build --no-isolation` and a virtual environment with
`system_site_packages=True`. Those checks could depend on the ambient editable-development
environment and therefore did not prove the release-candidate boundary they claimed.

B17-R1 keeps the product/runtime contract unchanged and narrows the verifier instead:

- wheel/sdist are now built through the declared isolated PEP 517 build environment;
- the install check creates a fresh venv with `system_site_packages=False` and its own pip;
- the built wheel is installed normally so its two runtime dependencies are resolved from
  wheel metadata inside that venv;
- NumPy and Pillow module paths are checked to live under the fresh venv; and
- captured subprocess output is emitted before a failing command is re-raised, so future
  packaging failures remain diagnosable in CI logs.

Remote workflow run `33629318890` then completed successfully, including both the B17
package/install gate and the dependent B18 freeze gate.

## Verification

- skill-creator `quick_validate.py`: valid;
- B17 source/docs/CI audit: pass;
- isolated PEP 517 wheel/sdist build: pass;
- wheel/sdist path, content, version, dependency, license/notice, English-only guidance,
  local-link, absolute-path, secret-pattern, and traversal audit: pass;
- truly isolated wheel install plus installed observed/subjectless examples: pass;
- installed NumPy/Pillow resolve from the fresh venv rather than host site-packages;
- frozen R23 compatibility boundary remains intact under `0.6.0rc1`;
- repository baseline and path/JSON/Python sanitization: pass;
- complete development regression in remote run `33629318890`: 212 passed, 2 existing
  `jsonschema.RefResolver` deprecation warnings;
- B17 package gate: success;
- B18 freeze gate: success.

## Reopen conditions

Reopen B17 if build/install/API/version truth diverges, artifacts include unapproved
evidence or paths, canonical docs route new work into R23/Pn, selected examples bypass the
installed package or fake reference evidence, dependencies/licenses drift without audit,
or CI stops exercising the release-candidate boundary.
