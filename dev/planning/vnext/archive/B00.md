# Archived B00 — Legacy R23 freeze + failure dossier

Status: **CLOSED**
This is the archived terminal B00 card. It is not an active production slice.

## Responsibility

Freeze R23 at `25ec454` as a reproducible legacy/reference baseline and collect the
evidence that justifies the vNext workflow reset.

## Why this slice now

- Impact: 5/5 — later vNext work must not inherit a misleading acceptance boundary.
- Uncertainty: 4/5 — the two dogfood runs fail in different ways.
- Reusability: 5/5 — the baseline boundary and failure evidence govern every later slice.
- Dependency override: hard prerequisite for all vNext implementation.

## Inputs

- `dev/planning/img2drawing-vnext-bottleneck-roadmap.md`, read in full.
- Frozen baseline `25ec4544e86fe37fc28d64575df145a1b711d63a`.
- Gemini, Claude, and R23 assessment source logs identified by stable IDs and hashes
  in `BASELINE.md` and the failure dossiers.
- Existing R23 classification in `dev/evidence/material-integration/` and `GATES.md`.

## Outputs

- `STATUS.md`
- `BASELINE.md`
- `failure-dossier/gemini.md`
- `failure-dossier/claude.md`
- `failure-dossier/r23-architecture.md`
- `capsules/B00.md`
- Read-only pin check: `verify_baseline.py`

## Explicit non-goals

- No B01 contract implementation.
- No Inspection Foundation or measurement implementation.
- No `DrawingSession` implementation.
- No runtime, renderer, schema, or legacy code changes.
- No vNext dogfood run and no automatic activation of B01.

## Definition of Closed

- [x] `25ec454` is fixed in documentation and mechanically verified by
      `verify_baseline.py`.
- [x] R23 KEEP / TRANSFORM / DEPRECATE disposition is explicit in
      `failure-dossier/r23-architecture.md`.
- [x] Gemini’s false-positive visual acceptance and Claude’s cost/inspection failure
      are recorded as distinct failure modes.
- [x] R23 artifacts are explicitly barred from automatic vNext PASS reuse.
- [x] The vNext goal is stated as better drawing with less review overhead, not more
      gates.
- [x] `capsules/B00.md` is written with authoritative source locations and reopen
      conditions.

## Verification record

The read-only baseline check passed against the exact object and current descendant
revision. The frozen Git object was not modified. Repository hygiene normalized
machine-local path metadata in historical R23 records and helper scripts to portable
references without changing runtime behavior, evidence claims, or coordinates.

## Closure decision

**CLOSED.** B01 remains SKELETON and requires a later explicit activation.
