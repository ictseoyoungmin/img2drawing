# S09 A/B/C ablation board

The harness compares subject+contract (A), the existing full-body exemplar (B),
and modular grammar cards (C) using structural metrics only. A card policy is
adopted only when C strictly improves every ordered metric and all trials are
tracked through P4.
