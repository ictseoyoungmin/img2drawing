# img2drawing R23 release candidate

This directory is generated from the current source tree after S10–S14 closure.
`release_manifest.json` is the hash authority for the ZIP/tree/package artifacts;
`validate_r23_release.py` checks the single version identity. The skill ZIP is
the distributable agent skill. Mechanical package checks do not replace the
independent visual evidence under `dev/evidence/`.
