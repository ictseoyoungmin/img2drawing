# B13 deterministic reference-authority fixture

This fixture creates observed, imaginative, and hybrid sessions through one
`DrawingSession`, authors the same small mechanical mark set, inspects each current
state, renders canonical output, and resumes every checkpoint.

```bash
PYTHONPATH=skills/img2drawing/src python3 dev/fixtures/vnext-b13/run.py --output /tmp/vnext-b13
```

The trace records authority, artifact availability, state continuity, and output. It is
mechanical authority/persistence evidence, not drawing-mode or visual-quality dogfood.
